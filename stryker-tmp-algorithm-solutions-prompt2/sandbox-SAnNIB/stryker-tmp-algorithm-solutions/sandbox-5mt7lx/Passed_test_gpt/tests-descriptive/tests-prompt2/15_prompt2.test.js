// @ts-nocheck
// 
import { solve } from '../../../solutions-descriptive/15.js';

test('Single element array', () => {
    expect(solve(1, 1, 1, [1])).toBe('1');
});

test('Minimum values', () => {
    expect(solve(1, 1, 1, [1])).toBe('1');
});